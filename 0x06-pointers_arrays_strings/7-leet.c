#include "holberton.h"

/**
 * leet - encodes strings into 1337
 *
 * @s: pointer to string
 *
 * Return: pointer
 */

char *leet(char *s)
{
	int i = 0;
	int n = 0;
	char lt[] = "a4A4e3E3o0O0t7T7l1L1"

	for (i = 0; s[i] != '\0'; i++)
	{
		for (n = 0; lt[n] != '\0'; n++)
		{
			if (s[i] == lt[n])
			{
				
			}
		}
	}

	return (s);
}
