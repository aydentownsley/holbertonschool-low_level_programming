#include "holberton.h"

/**
 * mul - multiplies the two inputs
 * @a: input one
 * @b: input two
 * Return: the product of a and b (which will be int c)
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
