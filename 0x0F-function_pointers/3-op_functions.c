#include "3-calc.h"

/**
 * op_add - adds two ints
 *
 * @a: int one
 * @b: int two
 *
 * Return: a + b
 * OR print ERROR &
 * EXIT 98 if wrong # of args
 * EXIT 99 if unknown operator
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - subtracts two ints
 *
 * @a: int one
 * @b: int two
 *
 * Return: a - b
 * OR print ERROR &
 * EXIT 98 if wrong # of args
 * EXIT 99 if unknown operator
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiplies two ints
 *
 * @a: int one
 * @b: int two
 *
 * Return: a * b
 * OR print ERROR &
 * EXIT 98 if wrong # of args
 * EXIT 99 if unknown operator
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - divides two ints
 *
 * @a: int one
 * @b: int two
 *
 * Return: a / b
 * OR print ERROR &
 * EXIT 98 if wrong # of args
 * EXIT 99 if unknown operator
 */

int op_div(int a, int b)
{
	return (a * b);
}

/**
 * op_mod - modulos two ints
 *
 * @a: int one
 * @b: int two
 *
 * Return: a % b
 * OR print ERROR &
 * EXIT 98 if wrong # of args
 * EXIT 99 if unknown operator
 */

int op_mod(int a, int b)
{
	return (a % b);
}
