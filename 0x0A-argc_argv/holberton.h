#ifndef _HOLBERTON_H_
#define _HOLBERTON_H_

/* Prototype of putchar */
int _putchar(char);

/* Prototype of islower */
int _islower(int);

/* Prototype of isalpha */
int _isalpha(int);

/* Prototype of abs */
int _abs(int);

/* Prototype of isupper */
int _isupper(int);

/* Prototype of isdigit */
int _isdigit(int);

/* Prototype of strlen */
int _strlen(char *);

/* Prototype of puts */
void _puts(char *);

/* Prototype of strcpy */
char *_strcpy(char *, char *);

/* Prototype of atoi */
int _atoi(char *);

/* Prototype of strcat */
char *_strcat(char *, char *);

/* Prototype of strncat */
char *_strncat(char *, char *, int);

/* Prototype of strncpy */
char *_strncpy(char *, char *, int);

/* Prototype of strcmp */
int _strcmp(char *, char *);

/* Prototype of memset */
char *_memset(char *, char, unsigned int);

/* Prototype of memcpy */
char *_memcpy(char *, char *, unsigned int);

/* Protytpe of strchr */
char *_strchr(char *, char);

/* Prototype of strspn */
unsigned int _strspn(char *, char *);

/* Prototype of strpbrk */
char *_strpbrk(char *, char *);

/* Prototype of strstr */
char *_strstr(char *, char *);

#endif /*_HOLBERTON_H_*/
